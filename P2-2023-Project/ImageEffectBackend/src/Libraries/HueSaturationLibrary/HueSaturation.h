#ifndef HUE_SATURATION_H
#define HUE_SATURATION_H
#include <bits/stdc++.h>
#include "../Pixel.h"

using namespace std;

// Function prototype for applying hue and saturation adjustment to an image
void HueSaturationImageEffect(vector<vector<Pixel>>& imageVector, float saturationValue, float hueValue);

#endif 
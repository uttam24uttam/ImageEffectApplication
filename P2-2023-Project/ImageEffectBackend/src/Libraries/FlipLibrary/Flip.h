#ifndef FLIP_H
#define FLIP_H
#include "../Pixel.h"
#include <bits/stdc++.h>

using namespace std;

// Function prototype for applying image flipping operations
void applyImageFlip(vector<vector<Pixel>>& imageVector, int horizontalFlipValue, int verticalFlipValue);

#endif
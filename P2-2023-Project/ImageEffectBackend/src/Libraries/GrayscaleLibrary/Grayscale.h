#ifndef GRAYSCALE_H
#define GRAYSCALE_H
#include "../Pixel.h"
#include <vector>

using namespace std;

// Function prototype for applying a grayscale effect to an image
void GrayscaleImageEffect(vector<vector<Pixel>>& imageVector);

#endif
